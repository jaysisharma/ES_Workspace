rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Match any document in the database
    match /{document=**} {
      // Allow read and write access for development & authenticated sessions
      allow read, write: if true;
    }
  }
}
